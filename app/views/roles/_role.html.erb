<tr id="<%= dom_id(role) %>">
	<td><%=h role.name%></td>
	<td><%= link_to "show", client_role_path(@client, role)%></td>
	<td><%= link_to_remote 'destroy', 
                    :url => client_role_path(@client, role), 
                    :confirm => 'Are you sure?', 
                    :method => :delete %></td>
</tr>